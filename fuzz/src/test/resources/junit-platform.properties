# Scope Jazzer's coverage instrumentation to this project's own packages instead of every class
# on the test classpath (JUnit, AssertJ, Mockito, ...). Instrumenting third-party code only adds
# noise to the coverage feedback the fuzzer steers by, and slows every execution down.
jazzer.instrument=io.github.dfa1.vortex.**
