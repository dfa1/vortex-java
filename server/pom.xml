<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.dfa1.vortex</groupId>
        <artifactId>vortex-java</artifactId>
        <version>0.14.2-SNAPSHOT</version>
    </parent>

    <artifactId>vortex-server</artifactId>

    <name>vortex-server</name>
    <description>Standalone, minimal object-storage HTTP server: GET (byte-range aware), HEAD, PUT,
        and a plain-text object listing over one local directory. Exists to demonstrate
        VortexHttpReader's partial-fetch story against something closer to real object storage
        than a static file server: the JDK's own jwebserver does not implement byte-range
        requests at all. Depends on nothing but the JDK: no Vortex module, so it is reusable
        outside this project too.</description>

    <properties>
        <!-- Demo/presentation tooling, not a consumable library: never published to Maven
             Central. maven.deploy.skip alone is not enough: central-publishing-maven-plugin
             takes over the deploy phase via <extensions>true</extensions> in the parent's release
             profile and runs regardless, with its own skip switch (see 74780aa3). -->
        <maven.deploy.skip>true</maven.deploy.skip>
        <skipPublishing>true</skipPublishing>
        <!-- Demo/presentation tooling: main(String[] args) and System.out logging dominate the
             Sonar findings here with no library-quality signal, same rationale as
             vortex-performance. -->
        <sonar.skip>true</sonar.skip>
    </properties>

    <dependencies>
        <!-- testing -->
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <finalName>vortex-server</finalName>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>io.github.dfa1.vortex.server.VortexServer</mainClass>
                                </transformer>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
