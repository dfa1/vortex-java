<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>io.github.dfa1.vortex</groupId>
		<artifactId>vortex-java</artifactId>
		<version>0.13.1-SNAPSHOT</version>
	</parent>

	<artifactId>vortex-reader</artifactId>

	<name>vortex-reader</name>
	<description>Reader for the Vortex columnar file format. Zero-copy memory-mapped reads via the Java FFM API (Java
		25+), no JNI, no sun.misc.Unsafe.
	</description>

	<properties>
		<!-- JUnit tags excluded from a routine `./mvnw test` / `./mvnw verify`. Defaults to `fuzz`
		     so the Jazzer @FuzzTest targets never run by default — see the surefire plugin config
		     below. Override on the CLI to opt in, e.g. `-Dvortex.reader.excludedGroups=` (empty)
		     runs every tag; a literal pom value could not be cleared this way. -->
		<vortex.reader.excludedGroups>fuzz</vortex.reader.excludedGroups>
	</properties>

	<dependencies>
		<!-- production -->
		<dependency>
			<groupId>io.github.dfa1.vortex</groupId>
			<artifactId>vortex-core</artifactId>
		</dependency>
		<dependency>
			<groupId>io.github.dfa1.vortex</groupId>
			<artifactId>vortex-fsst</artifactId>
		</dependency>
		<dependency>
			<groupId>io.github.dfa1.zstd</groupId>
			<artifactId>zstd</artifactId>
			<optional>true</optional>
		</dependency>
		<!-- testing -->
		<!-- project-internal test deps -->
		<dependency>
			<groupId>io.github.dfa1.vortex</groupId>
			<artifactId>vortex-core</artifactId>
			<type>test-jar</type>
			<scope>test</scope>
		</dependency>
		<!-- external test deps -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>com.code-intelligence</groupId>
			<artifactId>jazzer-junit</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-surefire-plugin</artifactId>
				<configuration>
					<!-- Exclude the Jazzer @FuzzTest targets from a routine `./mvnw test` /
					     `./mvnw verify`: fuzzing is opt-in and, under JAZZER_FUZZ=1, unbounded and
					     non-deterministic — not something a default build should run. Bound to a
					     property (not a literal) so the CLI can clear it:
					     `-Dvortex.reader.excludedGroups=` opts every fuzz target back in. This
					     exclusion filters even an explicit `-Dtest=VortexReaderFuzzTest` class
					     selection, so running a fuzz target always requires clearing the property
					     as well. -->
					<excludedGroups>${vortex.reader.excludedGroups}</excludedGroups>
				</configuration>
			</plugin>
			<!-- Publish a test-jar so writer/ tests can reuse reader test helpers
			     (TestRegistry, TestDecodeContexts, DecodeTestHelper). -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<executions>
					<execution>
						<id>publish-test-jar</id>
						<goals>
							<goal>test-jar</goal>
						</goals>
						<configuration>
							<!-- Keep this module's JUnit platform config out of the published
							     test-jar. JUnit reads whichever junit-platform.properties it finds
							     first on the classpath, so shipping ours would silently become the
							     config of every consumer of this test-jar (writer/). -->
							<excludes>
								<exclude>junit-platform.properties</exclude>
							</excludes>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- Mutation testing pilot (ADR: test-suite strength). Opt-in only:
		     ./mvnw -pl reader -am -P pitest verify -DskipITs
		     Common PIT setup is inherited from the parent `pitest` profile; only the scope differs:
		     the file-structure bounds-checking parse classes with dedicated security tests.
		     Report: reader/target/pit-reports/. -->
		<profile>
			<id>pitest</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.pitest</groupId>
						<artifactId>pitest-maven</artifactId>
						<configuration>
							<targetClasses>
								<param>io.github.dfa1.vortex.reader.Footer</param>
								<param>io.github.dfa1.vortex.reader.Trailer</param>
								<param>io.github.dfa1.vortex.reader.PostscriptParser</param>
								<param>io.github.dfa1.vortex.reader.SegmentSpec</param>
								<param>io.github.dfa1.vortex.reader.layout.Layout</param>
								<param>io.github.dfa1.vortex.reader.SerializedArrayDecoder</param>
							</targetClasses>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
