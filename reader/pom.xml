<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>io.github.dfa1.vortex</groupId>
		<artifactId>vortex-java</artifactId>
		<version>0.13.2-SNAPSHOT</version>
	</parent>

	<artifactId>vortex-reader</artifactId>

	<name>vortex-reader</name>
	<description>Reader for the Vortex columnar file format. Zero-copy memory-mapped reads via the Java FFM API (Java
		25+), no JNI, no sun.misc.Unsafe.
	</description>

	<dependencies>
		<!-- production -->
		<dependency>
			<groupId>io.github.dfa1.vortex</groupId>
			<artifactId>vortex-core</artifactId>
		</dependency>
		<dependency>
			<groupId>io.github.dfa1.vortex</groupId>
			<artifactId>vortex-fsst</artifactId>
		</dependency>
		<dependency>
			<groupId>io.github.dfa1.zstd</groupId>
			<artifactId>zstd</artifactId>
			<optional>true</optional>
		</dependency>
		<!-- testing -->
		<!-- project-internal test deps -->
		<dependency>
			<groupId>io.github.dfa1.vortex</groupId>
			<artifactId>vortex-core</artifactId>
			<type>test-jar</type>
			<scope>test</scope>
		</dependency>
		<!-- external test deps -->
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.assertj</groupId>
			<artifactId>assertj-core</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.mockito</groupId>
			<artifactId>mockito-junit-jupiter</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<!-- Publish a test-jar so writer/ tests can reuse reader test helpers
			     (TestRegistry, TestDecodeContexts, DecodeTestHelper). -->
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<executions>
					<execution>
						<id>publish-test-jar</id>
						<goals>
							<goal>test-jar</goal>
						</goals>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>

	<profiles>
		<!-- Mutation testing pilot (ADR: test-suite strength). Opt-in only:
		     ./mvnw -pl reader -am -P pitest verify -DskipITs
		     Common PIT setup is inherited from the parent `pitest` profile; only the scope differs:
		     the file-structure bounds-checking parse classes with dedicated security tests.
		     Report: reader/target/pit-reports/. -->
		<profile>
			<id>pitest</id>
			<build>
				<plugins>
					<plugin>
						<groupId>org.pitest</groupId>
						<artifactId>pitest-maven</artifactId>
						<configuration>
							<targetClasses>
								<param>io.github.dfa1.vortex.reader.Footer</param>
								<param>io.github.dfa1.vortex.reader.Trailer</param>
								<param>io.github.dfa1.vortex.reader.PostscriptParser</param>
								<param>io.github.dfa1.vortex.reader.SegmentSpec</param>
								<param>io.github.dfa1.vortex.reader.layout.Layout</param>
								<param>io.github.dfa1.vortex.reader.SerializedArrayDecoder</param>
							</targetClasses>
						</configuration>
					</plugin>
				</plugins>
			</build>
		</profile>
	</profiles>
</project>
