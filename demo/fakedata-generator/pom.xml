<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.github.dfa1.vortex</groupId>
        <artifactId>vortex-java</artifactId>
        <version>0.14.2</version>
        <relativePath>../../pom.xml</relativePath>
    </parent>

    <artifactId>vortex-fakedata-generator</artifactId>

    <name>vortex-fakedata-generator</name>
    <description>Standalone CLI: generates a Vortex file of synthetic data from a compact
        column-description grammar (name:type:generator(args), e.g.
        "price:f64:range(50,150)"), inspired by PostgreSQL's generate_series. Independent of
        any particular demo -- reusable wherever a quick synthetic .vortex file is needed.</description>

    <properties>
        <!-- Demo/presentation tooling, not a consumable library: never published to Maven
             Central. maven.deploy.skip alone is not enough: central-publishing-maven-plugin
             takes over the deploy phase via <extensions>true</extensions> in the parent's
             release profile and runs regardless, with its own skip switch (see 74780aa3). -->
        <maven.deploy.skip>true</maven.deploy.skip>
        <skipPublishing>true</skipPublishing>
        <!-- Demo/presentation tooling: main(String[] args) and a CLI argument parser dominate
             the Sonar findings here with no library-quality signal, same rationale as
             vortex-performance. -->
        <sonar.skip>true</sonar.skip>
    </properties>

    <dependencies>
        <!-- production -->
        <dependency>
            <groupId>io.github.dfa1.vortex</groupId>
            <artifactId>vortex-writer</artifactId>
        </dependency>
        <!-- testing -->
        <dependency>
            <groupId>io.github.dfa1.vortex</groupId>
            <artifactId>vortex-reader</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.assertj</groupId>
            <artifactId>assertj-core</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <finalName>vortex-fakedata-generator</finalName>
                            <transformers>
                                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>io.github.dfa1.vortex.demo.fakedata.FakeDataGeneratorCli</mainClass>
                                </transformer>
                            </transformers>
                            <filters>
                                <filter>
                                    <artifact>*:*</artifact>
                                    <excludes>
                                        <exclude>META-INF/*.SF</exclude>
                                        <exclude>META-INF/*.DSA</exclude>
                                        <exclude>META-INF/*.RSA</exclude>
                                    </excludes>
                                </filter>
                            </filters>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
